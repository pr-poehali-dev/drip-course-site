import React from 'react';
import { Link } from 'react-router-dom';
import { Card, CardContent, CardDescriptionПонятно, про, CardFooter, CardHeader, CardTitle } from '../components/ui/card';
import { Button } from '../components/ui/button';
import {блема возникает после согласия на обработку данных. Дав CheckCircle } from айте это исправим.'lucide-react Проверю';

const ThankYouPage: React компоненты и ма.FC = () =>ршрутизац {
  return (
    <div className="container max-wию.

<pp-code>-3xl mx-auto px-4 
<pp-filepathpy-8">
      <Card className=">src/types/bg-white shadow-lg">
        types.ts</pp-<CardHeader className="textfilepath>
<pp-center bg-primary-content>export text-white rounded-t-lg">
          <CardTitle className type Answer = {
  question="text-2xl font-bold">Id: number;
  optСпасибо!</CardTitle>
          ionIndex: number;
};<CardDescription className="text-primary-fore

export type UserDataground">
             = {
  firstName: string;
  Ваши данные успешно отправленыmiddleName: string;
          </CardDescription>
  phone: string;
  
        </CardHeader>
        
        <CardContent className="p-6 textresult: ResultType;
};-center">
          

export type ResultType =<div className="flex 'ДЕТОКС' |  justify-center mb-6'СНИЖЕНИЕ">
            <div className="rounded-full bg-green ВЕСА' | -100 p-'ЭНЕРГИЯ';

export type Question =3 mb-4 {
  id: number;
  text">
              <CheckCircle className="h: string;
  options: string[];-16 w-16 text-green-600
};
